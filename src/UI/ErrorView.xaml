<UserControl x:Class="Assignment.UI.ErrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Assignment.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding Title}" Margin="10"/>
        <TextBlock Text="{Binding Message}" Margin="10"/>
        <Button Content="Ok" Margin="10" Command="{Binding CloseCommand}"/>
    </StackPanel>
</UserControl>
